<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <EditText android:id="@+id/etNombre" android:hint="Nombre del producto"/>
    <EditText android:id="@+id/etNumeroSerie" android:hint="Número de serie"/>
    <EditText android:id="@+id/etTipo" android:hint="Tipo"/>
    <EditText android:id="@+id/etFechaLlegada" android:hint="Fecha de llegada"/>

    <Button android:id="@+id/btnGuardarProducto" android:text="Guardar Producto"/>
</LinearLayout>
